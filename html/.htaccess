RewriteEngine On

# Evita redirigir a archivos o carpetas reales
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Reescribe la URL amigable a index.php?ruta=loquesea
RewriteRule ^([a-zA-Z0-9_-]+)$ index.php?ruta=$1 [QSA,L]
